class Circle {
  // double radius;
  // Circle(this.radius);

  // double AreaOfCircle() {
  //   return 3.14 * radius * radius;
  // }
  double AreaOfCircle({radius = 0}) => (3.1417 * radius * radius);
}
